[build]
  base    = "packages/landing"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[headers]]
  for = "/*"
  [headers.values]
    Basic-Auth = "propagate:$BASIC_AUTH_PASSWORD"
